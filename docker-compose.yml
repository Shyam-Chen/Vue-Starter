version: '3.9'

services:
  ci:
    image: ci
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
      - 3000:3000
    tty: true

  app:
    image: app
    build:
      context: .
      dockerfile: .circleci/Dockerfile
    ports:
      - 3000:3000
