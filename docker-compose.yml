version: '3.9'

services:
  app:
    image: app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5173:5173
    volumes:
      - ./:/vue-starter
    tty: true

  ci:
    image: ci
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    tty: true
