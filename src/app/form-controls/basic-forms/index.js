import BasicForms from './BasicForms';
import { INITIAL as state } from './constants';
import mutations from './mutations';

const basicForms = {
  state,
  mutations
};

export { BasicForms, basicForms };
